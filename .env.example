# Firebase 설정
# Firebase Console에서 프로젝트 설정 > 일반 탭에서 확인 가능
# https://console.firebase.google.com/

VITE_FIREBASE_API_KEY=your-api-key-here
VITE_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_APP_ID=1:123456789:web:abcdefghijk

# 주의사항:
# 1. .env 파일을 생성하고 위의 값을 실제 Firebase 설정으로 교체하세요
# 2. Firebase Console > Authentication에서 Google 로그인 활성화
# 3. Google Cloud Console에서 Google Drive API 활성화
# 4. OAuth consent screen에서 Drive API scope 추가
# 5. .env 파일은 절대 Git에 커밋하지 마세요 (.gitignore에 포함되어 있음)

# Firebase API Key는 공개되어도 안전합니다 (Firebase Security Rules로 보호됨)

